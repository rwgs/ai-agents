interface:
  display_name: "PR Readiness"
  short_description: "Check a change is finished, locally or on a pull request"
  default_prompt: "Use $pr-readiness to validate this change, review it locally, and decide whether it is ready to merge."
