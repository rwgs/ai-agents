interface:
  display_name: "Bash Scripting"
  short_description: "Write, review, and debug safe portable shell scripts"
  default_prompt: "Use $bash-scripting to write, review, or debug a shell script safely."
