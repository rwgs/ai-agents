name: Validate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unix:
    name: Validate (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Run ShellCheck
        if: runner.os == 'Linux'
        run: shellcheck scripts/*.sh

      - name: Validate repository
        run: ./scripts/validate.sh

  windows:
    name: Validate (windows-latest)
    runs-on: windows-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Run PSScriptAnalyzer
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'
          if (-not (Get-Module -ListAvailable PSScriptAnalyzer)) {
            Set-PSRepository PSGallery -InstallationPolicy Trusted
            Install-Module PSScriptAnalyzer -Scope CurrentUser -Force
          }
          # The installers expose -DryRun, which README documents, so they do not
          # also implement the -WhatIf that ShouldProcess would require.
          $findings = @(
            Invoke-ScriptAnalyzer -Path scripts -Severity Warning, Error `
              -ExcludeRule PSUseShouldProcessForStateChangingFunctions
          )
          if ($findings.Count) {
            $findings |
              Format-Table RuleName, ScriptName, Line, Message -AutoSize |
              Out-String -Width 200 |
              Write-Output
            throw "PSScriptAnalyzer reported $($findings.Count) finding(s)."
          }
          Write-Output 'PSScriptAnalyzer: no findings'

      - name: Test PowerShell installer
        shell: pwsh
        run: ./scripts/test-install.ps1

  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Review dependency changes
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high
